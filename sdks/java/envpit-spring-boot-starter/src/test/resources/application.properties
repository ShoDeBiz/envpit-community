# Used by EnvpitEnvironmentPostProcessorTest#envpitWinsOverApplicationPropertiesPackagedFile —
# a real ConfigData-loaded file (not a stand-in via SpringApplicationBuilder#properties(...)),
# proving EnvPit's PropertySource (added addAfter systemEnvironment) outranks a packaged
# application.properties file, per the precedence documented in ../../../README.md.
DATABASE_URL=postgres://from-packaged-file
